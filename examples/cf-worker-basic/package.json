{
  "name": "varlock-cf-workers-basic",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "npm run build && vite preview",
    "deploy": "APP_ENV=prod npm run build && wrangler deploy",
    "wrangler:types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.16.0",
    "@varlock/vite-integration": "^0.2.3",
    "varlock": "^0.4.0",
    "vite": "^7.2.6",
    "wrangler": "^4.52.0"
  }
}
